@model IEnumerable<Domain.RoomPolicies>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Room)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Policy)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Room.RoomId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Policy.PolicyId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RoomPoliciesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RoomPoliciesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RoomPoliciesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
